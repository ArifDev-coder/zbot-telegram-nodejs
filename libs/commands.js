const commands = {
    halo: /^!halo$/,
    follow: /^!follow (.+)/,
    motivation: /^!motivation (.+)/,
    news: /^!news$/,
    quake: /^!quake$/,
    help: /^!help$/,
    author: /^!author$/,
}

module.exports = commands;